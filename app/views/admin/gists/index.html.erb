<h1>Gists</h1>

<table>
  <thead>
    <tr>
      <th><%= t('gists.index.question_header') %></th>
      <th><%= t('gists.index.url_header') %></th>
      <th><%= t('gists.index.user_header') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @gists %>
  </tbody>
</table>
